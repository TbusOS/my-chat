# 动画 #60：大模型训练·数据硬件之旅 — 设计文档

> 用 3D 视角追踪训练数据的完整旅程——从硬盘出发，穿越 CPU、PCIe 通道，进入 GPU 的 HBM 显存，深入 SM 流处理器内部的 Tensor Core 完成矩阵运算，最后多卡之间通过 NVLink 同步梯度。像一部微观纪录片，追踪"一个 token 的冒险之旅"。

---

## 概述

| 项目 | 内容 |
|------|------|
| 文件名 | `60-大模型训练数据硬件之旅.html` |
| 场景数 | 17 个，分 6 大阶段 |
| 技术栈 | 纯 HTML + CSS + vanilla JS，Canvas 2D + 3D 投影数学 |
| 字体 | Google Fonts — Noto Sans SC + JetBrains Mono |
| 渲染方案 | 沿用 #58 的 3D 引擎：旋转矩阵 + 透视投影 + 粒子系统 |
| 预估代码量 | ~2000-2500 行 |

---

## 阶段与场景总览

```
第一阶段：数据准备          Scene 1-3    NVMe → CPU 预处理
第二阶段：数据传输          Scene 4-5    PCIe 通道 → GPU 芯片全貌
第三阶段：GPU 存储层次      Scene 6-8    HBM 塔 → 存储金字塔 → SM 内部
第四阶段：计算过程          Scene 9-13   Tensor Core → Embedding → Attention → FlashAttention → FFN
第五阶段：反向传播与同步    Scene 14-16  梯度逆流 → 混合精度 → AllReduce
第六阶段：更新与循环        Scene 17     Adam 优化器 → 下一轮训练
```

---

## 第一阶段：数据准备（Scene 1-3）

### Scene 1 — 训练系统全景

**视觉目标**：建立全局认知，让观众一眼看清训练系统的硬件组成。

**3D 效果**：
- 一个发光的服务器机架，等距 3D 视角
- 内部可见：CPU 主板 + 8 块 GPU 卡排列 + 顶部 NVMe SSD 阵列
- 各组件之间有发光连线，标注总线类型和带宽
- 数据粒子从 NVMe 端开始闪烁，预示旅程即将开始

**交互**：
- 鼠标拖拽旋转整个机架
- 各组件悬浮标签：CPU / DDR5 / PCIe Switch / GPU×8 / NVLink / NVMe

**标注数据**：
- 系统总览图，无具体数据，重在展示拓扑关系

---

### Scene 2 — 数据从磁盘出发：NVMe 内部

**视觉目标**：展示数据的"出生地"——NVMe SSD 的 3D NAND 内部结构。

**3D 效果**：
- 放大进入 NVMe SSD 内部
- 3D NAND Flash 存储单元阵列：8-16 层发光平面垂直堆叠
- 控制器芯片从 NAND 层读出 token 数据
- 数据粒子从存储单元中被"提取"出来，经 NVMe 协议封装后发出

**交互**：
- **按钮「读取数据」**：触发一批 token 数据从 NAND 中流出的动画

**标注数据**：
- NVMe SSD 读取带宽：~7 GB/s
- 3D NAND 层数：64-128 层

---

### Scene 3 — CPU 预处理工厂

**视觉目标**：展示 CPU 如何并行处理训练数据，以及 CPU 内部的缓存层次。

**3D 效果**：
- CPU die 的 3D 俯视图
- 多个核心（方块阵列）同时工作，每个核心 = 一个 DataLoader Worker
- 核心周围环绕三级缓存同心环：L1(最内) → L2(中) → L3(最外)
- DDR5 内存条在旁边竖立，数据通过内存通道流入

**工作流可视化**：
- 读取原始文本 → Tokenization(分词) → Padding(填充) → 组成 Batch(批次)

**交互**：
- **按钮「启动 DataLoader」**：多个核心同时亮起，并行处理数据

**标注数据**：
- DDR5 带宽：~90 GB/s
- L3 Cache 带宽：~300 GB/s
- DataLoader Workers：通常 4-8 个

---

## 第二阶段：数据传输（Scene 4-5）

### Scene 4 — PCIe 高速通道

**视觉目标**：展示 CPU 到 GPU 的"高速公路"，直观感受传输带宽。

**3D 效果**：
- 一条发光隧道连接 CPU 端和 GPU 端
- 隧道内部可见 16 条并行数据通道（PCIe x16 lanes）
- 数据粒子在各通道中以波浪形飞速前进，带拖尾效果
- 隧道壁上有速度仪表盘
- 隧道颜色从 CPU 端（蓝色）渐变到 GPU 端（金色）

**交互**：
- **按钮「对比 Gen4/Gen5」**：切换隧道宽度和粒子速度，直观感受代差

**标注数据**：
- PCIe Gen4 x16：32 GB/s
- PCIe Gen5 x16：64 GB/s
- PCIe Gen6 x16：128 GB/s（下一代）

---

### Scene 5 — GPU 芯片鸟瞰

**视觉目标**：第一次看到 GPU 芯片的全貌，建立空间感。

**3D 效果**：
- H100 芯片的 3D die 布局，从高处俯瞰
- 中央：80 个 SM 组成的密集网格（金色方块阵列），微微发光脉动
- 四周：6 组 HBM3 堆叠如高塔矗立在芯片边缘（蓝色）
- 中间：L2 Cache 环形分布在 SM 和 HBM 之间（青色光带）

**交互**：
- 鼠标拖拽旋转观察芯片
- **按钮「高亮各区域」**：依次点亮 SM → L2 → HBM，展示数据路径方向

**标注数据**：
- 80 个 SM (Streaming Multiprocessor)
- 528 个 Tensor Cores（每 SM 含 4 个第四代 Tensor Core）
- 50 MB L2 Cache
- 80 GB HBM3 显存

---

## 第三阶段：GPU 存储层次（Scene 6-8）

### Scene 6 — HBM 存储塔

**视觉目标**：展示 HBM 的 3D 堆叠架构，理解"高带宽"从何而来。

**3D 效果**：
- 放大一组 HBM3 堆叠
- 8-12 层 DRAM die 垂直堆叠，每层半透明可见内部存储阵列
- 底部是 Logic Die（控制层）
- 层间由数千根 TSV（硅通孔）柱子连接——细长发光柱
- 数据粒子通过 TSV 在层间垂直穿行

**交互**：
- **按钮「数据写入」**：一批训练数据（权重 + 输入）被写入各层，粒子沿 TSV 下沉

**标注数据**：
- HBM3 带宽：3.35 TB/s
- 总线宽度：1024-bit（vs DDR5 的 64-bit，宽 16 倍）
- TSV (Through-Silicon Via)：硅通孔，直径 ~5-10μm
- 堆叠层数：8-12 层 DRAM die

---

### Scene 7 — 存储层次金字塔

**视觉目标**：一图对比 GPU 四级存储的容量、带宽、延迟差异。

**3D 效果**：
- 一座四层 3D 金字塔，从底到顶：
  - 底层（最大）：HBM — 深蓝色
  - 第二层：L2 Cache — 青色
  - 第三层：Shared Memory / L1 — 青绿色
  - 顶层（最小）：Register File — 金色
- 每层大小逐渐缩小，发光强度递增（越快越亮）
- 右侧动态柱状图对比：容量(对数轴) vs 带宽 vs 延迟
- 数据粒子从底部逐层向上攀升，每穿过一层速度加快、颜色变亮

**交互**：
- 点击金字塔每层高亮并显示详细信息
- **按钮「逐层穿越」**：数据粒子从 HBM 底层一路上升到 Register，体验速度差异

**标注数据**：

| 层级 | 容量 | 带宽 | 延迟 |
|------|------|------|------|
| HBM3 | 80 GB | 3.35 TB/s | ~600 cycles |
| L2 Cache | 50 MB | ~6 TB/s | ~200 cycles |
| Shared Memory / L1 | 228 KB / SM | ~19 TB/s | ~30 cycles |
| Register File | 256 KB / SM | ∞（零延迟） | 1 cycle |

---

### Scene 8 — 深入 SM 内部

**视觉目标**：展示 GPU 最核心的计算单元——SM 的内部架构。

**3D 效果**：
- 进入一个 SM 的 3D 内部空间
- 顶部：Warp Scheduler × 4（指挥中心，发出调度光线）
- 中部：128 个 CUDA Core 排列成 4 排 × 32 列的蓝色小立方体阵列
- 中央突出：4 个 Tensor Core（大金色立方体，比 CUDA Core 大 4 倍）
- 底部：Shared Memory + Register File（发光平面层）

**数据流动**：
- Warp Scheduler 发出调度指令（光线射向不同 Core）
- 数据从 Shared Memory → Register File → 分发到各 Core
- CUDA Core 按 Warp（32 个一组）分组高亮执行

**交互**：
- **按钮「Warp 调度」**：展示一个 Warp（32 线程）被调度到 CUDA Core 上同时执行的 SIMT 过程

**标注数据**：
- 1 SM = 4 Warp Schedulers + 128 CUDA Cores + 4 Tensor Cores
- 1 Warp = 32 线程，SIMT（单指令多线程）同步执行
- Register File：256 KB / SM（最快的存储）

---

## 第四阶段：计算过程（Scene 9-13）

### Scene 9 — Tensor Core 矩阵引擎

**视觉目标**：深入 Tensor Core，展示矩阵乘加运算的微观过程。

**3D 效果**：
- 深入一个 Tensor Core 内部
- 两个 4×4 矩阵从两侧滑入：
  - 矩阵 A（青色）从左侧滑入
  - 矩阵 B（橙色）从上方滑入
- 中心区域执行乘加运算 D = A × B + C：
  - 乘法过程：A 的行与 B 的列对应元素连线发光，汇聚到结果位置
  - 累加过程：矩阵 C（绿色）从下方叠加
- 输出矩阵 D（金色）从底部生成滑出

**交互**：
- **按钮「单步执行」**：逐步展示矩阵乘法每一步（逐行×逐列 → 点积 → 累加 → 输出）

**标注数据**：
- 1 Tensor Core = 64 FMA ops / clock
- 每周期完成一个 4×4 矩阵乘加
- 混合精度：FP16 输入 × FP16 输入 + FP32 累加器
- H100 全芯片：528 Tensor Cores，理论 ~990 TFLOPS (FP16)

---

### Scene 10 — 前向传播：Embedding 层

**视觉目标**：展示训练的第一步——Token ID 如何变成向量。

**3D 效果**：
- 左侧：一列 Token ID（发光数字序列，如 [1542, 8837, 256, ...]）
- 右侧：巨大的 Embedding 权重矩阵（存储在 HBM 中，发光网格）
  - 行数 = 词表大小（~128K）
  - 列数 = 隐藏维度（~4096）
- 每个 Token ID 发出一道光线射入矩阵，命中对应的行
- 被命中的行发光并"飞出"到输出端，组成 Embedding 向量序列

**数据路径**：
- HBM（权重矩阵） → L2 Cache → SM Shared Memory → Register → 计算

**交互**：
- **按钮「Token 查找」**：动画展示 3 个 token 同时在矩阵中查找对应行

**标注数据**：
- 词表大小：~128K（如 LLaMA 的 128,256）
- 隐藏维度：~4096（LLaMA 7B）或 ~12288（LLaMA 70B）
- Embedding 权重矩阵大小：128K × 4096 × 2B(FP16) ≈ 1 GB

---

### Scene 11 — 前向传播：自注意力机制

**视觉目标**：展示 Transformer 最核心的自注意力计算，以及数据如何在 Tensor Core 上完成。

**3D 效果**：
- 输入向量序列通过三组权重矩阵分别投影：
  - Q（红色粒子流）— Query 查询
  - K（蓝色粒子流）— Key 键
  - V（绿色粒子流）— Value 值
- Q × K^T 产生注意力分数矩阵：
  - 3D 热力图可视化，颜色深浅表示注意力强弱
- Softmax 归一化：数值归一化的波浪动画效果
- 注意力矩阵 × V → 加权输出
- 底部高亮 Tensor Core，所有矩阵运算都通过它完成

**交互**：
- **按钮「多头展开」**：展示多头注意力（Multi-Head Attention），多组 Q/K/V 并行工作，最后拼接

**标注数据**：
- Q/K/V 投影：各需一次矩阵乘法（调用 Tensor Core）
- 注意力矩阵大小：seq_len × seq_len（长序列时巨大）
- 多头数：32 头（LLaMA 7B）或 64 头（LLaMA 70B）

---

### Scene 12 — FlashAttention 优化对比

**视觉目标**：直观展示 FlashAttention 为什么能大幅减少内存访问。

**3D 效果 — 左右分屏**：

**左侧「标准 Attention」**：
- 完整 Q/K/V 矩阵全部从 HBM 读到 SM
- 计算注意力矩阵 → 写回 HBM
- 读出注意力矩阵 + V → 计算输出 → 写回 HBM
- 大量红色箭头在 HBM 和 SM 之间来回穿梭
- HBM 发热变红，表示访问压力大

**右侧「FlashAttention」**：
- Q/K/V 分成小块（tiling），逐块加载到 SRAM
- 在 SRAM 内完成该块的全部注意力计算
- 只将最终结果写回 HBM 一次
- 绿色分块在 SRAM 中高效处理
- HBM 保持冷蓝色，访问量极少

**交互**：
- **按钮「切换模式」**：在标准和 Flash 之间切换对比
- 动态计数器显示 HBM 访问次数对比

**标注数据**：
- 标准 Attention HBM 访问量：O(N²)
- FlashAttention HBM 访问量：O(N)
- 内存访问减少：10-20×
- 训练速度提升：2-4×
- SRAM 分块大小：由 SRAM 容量决定

---

### Scene 13 — 前向传播：FFN 层

**视觉目标**：展示 FFN（前馈网络）的维度变换和激活值存储。

**3D 效果**：
- 数据通过两层全连接网络：
  - 第一层 Linear：维度从 4096 扩展到 16384（粒子数量爆增，向外扩散）
  - 中间 GELU 激活函数：粒子经过波浪形变换（负值被抑制，正值保留）
  - 第二层 Linear：维度从 16384 压缩回 4096（粒子汇聚回来）
- 激活值写回 HBM：大量橙色数据"下沉"到底部 HBM 层
- 可视化 HBM 占用率增长

**交互**：
- **按钮「显示激活值存储」**：高亮 HBM 中激活值的占用区域，展示随层数增加的内存压力

**标注数据**：
- FFN 隐藏维度：通常为模型维度的 4 倍
- FFN 占 Transformer 总计算量的 ~2/3
- 激活值存储：每层每 token 需保存中间结果，占大量 HBM
- GELU 激活函数：比 ReLU 更平滑，训练效果更好

---

## 第五阶段：反向传播与同步（Scene 14-16）

### Scene 14 — 反向传播：梯度逆流

**视觉目标**：展示梯度如何从 Loss 端逆流回每一层，以及激活值的重新利用。

**3D 效果**：
- 镜头从 Loss 函数端开始（顶部红色脉冲源）
- 梯度粒子（粉红色）逆向流过每一层 Transformer
- 每经过一层：
  - 之前保存的激活值（橙色）从 HBM "上浮"到 SM
  - 梯度在该层分裂增殖（链式法则：梯度 = 本层梯度 × 上层传入梯度）
  - 该层的权重梯度写入 HBM 的梯度区域
- HBM 使用量条形图：权重（蓝） + 激活值（橙） + 梯度（粉） + 优化器状态（琥珀）

**交互**：
- **按钮「梯度检查点」**：展示 Gradient Checkpointing 优化——
  - 前向时只保存部分层的激活值
  - 反向时重新计算未保存的激活值
  - HBM 占用大幅减少，但计算量增加 ~33%

**标注数据**：
- 梯度大小 ≈ 参数大小（每个参数对应一个梯度值）
- 70B 模型梯度：~140 GB（FP32）或 ~70 GB（FP16）
- 激活值是 HBM 占用的主要来源
- Gradient Checkpointing：节省 ~60% 激活值内存，多 ~33% 计算

---

### Scene 15 — 混合精度训练

**视觉目标**：展示 FP32/FP16/BF16 不同精度如何协作，为什么能既省内存又不损失精度。

**3D 效果**：
- 三种数据块大小对比：
  - FP32 权重（大方块，32 bit，蓝色）
  - FP16 工作副本（小方块，16 bit，青色）
  - BF16 替代方案（小方块，16 bit，绿色）
- 数据流：
  - FP32 Master Weights → Cast 转换 → FP16 Working Copy
  - FP16 输入 Tensor Core → FP32 累加器 → FP16 输出
  - FP16 梯度 → Loss Scaling（放大镜效果）→ 防止梯度下溢
  - 梯度 → 转回 FP32 → 更新 Master Weights

**交互**：
- **按钮「精度对比」**：切换显示 FP32 全精度 vs 混合精度的内存占用和速度差异

**标注数据**：
- FP32：32 bit，精度高，内存大
- FP16：16 bit，精度较低但 Tensor Core 原生支持
- BF16：16 bit，指数位多（8 bit vs FP16 的 5 bit），范围大，训练更稳定
- 混合精度节省：~50% 显存，速度提升 2-3×
- Loss Scaling：梯度乘以 1024-65536，防止 FP16 下溢

---

### Scene 16 — 多卡互联：NVLink 与 AllReduce

**视觉目标**：展示多 GPU 如何通过 NVLink 高速同步梯度。

**3D 效果**：
- 8 张 GPU 在 3D 空间中排列成环形拓扑
- GPU 之间用发光的 NVLink 光束连接（紫色）
- 中央可选 NVSwitch 枢纽（全互联模式）

**AllReduce 动画（两阶段）**：

**Phase 1 — ReduceScatter**：
- 每张 GPU 将自己的梯度分成 8 份
- 各 GPU 沿环发送自己的碎片，同时接收并累加邻居的碎片
- 粒子在环上顺时针流动
- 每经过一站颜色加深（梯度被累加）
- 经过 7 轮传递，每张 GPU 持有 1/8 梯度的完整累加结果

**Phase 2 — AllGather**：
- 累加完成的梯度碎片再沿环传播
- 每个 GPU 收集到全部结果
- 粒子在环上流动，每站复制一份
- 最终每张 GPU 拥有完整的平均梯度

**交互**：
- **按钮「启动 AllReduce」**：触发完整的 ReduceScatter → AllGather 动画
- **按钮「NVLink vs PCIe」**：并排显示两种互联的速度差异

**标注数据**：
- NVLink 4.0（H100）：900 GB/s 双向
- PCIe Gen5：64 GB/s（NVLink 快 14 倍）
- AllReduce 通信量：2 × (K-1)/K × 梯度大小（K = GPU 数量）
- 8 卡 AllReduce：通信量 ≈ 1.75 × 梯度大小

---

## 第六阶段：更新与循环（Scene 17）

### Scene 17 — 优化器更新 & 训练循环

**视觉目标**：展示 Adam 优化器的工作过程，以及训练循环的完整闭环。

**3D 效果**：
- HBM 中三组数据并排展示：
  - 梯度 g（粉色块）
  - 一阶动量 m（蓝色块）
  - 二阶方差 v（绿色块）
- 优化器工作动画：
  - 读取 g, m, v
  - 计算更新：m = β₁m + (1-β₁)g
  - 计算更新：v = β₂v + (1-β₂)g²
  - 偏差修正：m̂ = m/(1-β₁^t), v̂ = v/(1-β₂^t)
  - 权重更新：w = w - lr × m̂ / (√v̂ + ε)
  - 新权重写回 HBM
- 完成后镜头缓缓拉远，回到 Scene 1 的全景视角
- 新一批数据从 NVMe 出发，Epoch 计数器 +1
- 循环动画，训练永不停歇

**交互**：
- **按钮「下一个 Epoch」**：触发循环动画，新数据从 NVMe 流入，整个流程重新开始

**标注数据**：
- Adam 优化器状态：每个参数需维护 m 和 v（各 FP32）
- 70B 模型优化器状态：70B × 2 × 4B = 560 GB
- 加上权重本身（FP32）：70B × 4B = 280 GB
- 总计：权重 + 梯度 + 优化器 = 280 + 140 + 560 = 980 GB → 需要多卡分摊
- 学习率 lr 通常 1e-4 ~ 3e-4
- β₁ = 0.9, β₂ = 0.999, ε = 1e-8

---

## 颜色系统

| 元素 | 颜色代码 | 用途 |
|------|---------|------|
| 原始数据 / Token | `#22d3ee` 青色 | 输入数据全程 |
| 权重矩阵 | `#4a9eff` 蓝色 | 模型参数 |
| Tensor Core | `#ffd666` 金色 | 核心计算引擎 |
| CUDA Core | `#64748b` 蓝灰色 | 通用计算 |
| 激活值 | `#fb923c` 橙色 | 前向传播中间产物 |
| 梯度 | `#f472b6` 粉色 | 反向传播 |
| NVLink 光束 | `#a78bfa` 紫色 | GPU 互联 |
| PCIe 通道 | `#4ade80` 绿色 | CPU ↔ GPU 传输 |
| HBM 存储 | `#1e40af` 深蓝色 | 显存 |
| SRAM / Cache | `#2dd4bf` 青绿色 | 片上高速缓存 |
| 优化器状态 | `#f59e0b` 琥珀色 | Adam m/v |
| Loss / 损失 | `#ef4444` 红色 | Loss 函数 |

---

## 术语表（❓ 按钮，16 个术语）

| 术语 | 英文全称 | 一句话解释 |
|------|---------|-----------|
| **HBM** | High Bandwidth Memory | 高带宽内存，3D 堆叠 DRAM，GPU 主显存，H100 配备 80GB |
| **SRAM** | Static Random-Access Memory | 静态随机存储，GPU 片上缓存，速度极快但仅 KB 级 |
| **SM** | Streaming Multiprocessor | 流多处理器，GPU 基本计算单元，H100 有 80 个 |
| **Tensor Core** | — | 张量核心，专用 4×4 矩阵乘加单元，AI 训练的核心引擎 |
| **CUDA Core** | — | 通用并行计算核心，处理标量/向量运算 |
| **Warp** | — | 线程束，32 个线程同步执行的最小调度单位 |
| **NVLink** | — | NVIDIA 高速 GPU 互联总线，H100 双向 900 GB/s |
| **NVSwitch** | — | GPU 互联交换机，实现全互联拓扑 |
| **PCIe** | PCI Express | CPU 与 GPU 之间的标准通信总线 |
| **TSV** | Through-Silicon Via | 硅通孔，HBM 层间垂直互联技术 |
| **AllReduce** | — | 分布式训练中梯度全局聚合操作 |
| **FlashAttention** | — | 利用 SRAM 分块计算注意力，减少 HBM 访问的优化算法 |
| **混合精度** | Mixed Precision Training | FP16 计算 + FP32 存储，兼顾速度与精度 |
| **梯度检查点** | Gradient Checkpointing | 不保存激活值，反向时重算，用计算换内存 |
| **前向传播** | Forward Pass | 输入数据逐层计算得到预测结果 |
| **反向传播** | Backward Pass | 从损失函数反向计算各层参数梯度 |

---

## 交互设计

### 导航系统
- 底部导航栏：17 个进度点 + 前进/后退箭头按钮
- 进度点上方显示当前阶段名称（如"第三阶段：GPU 存储层次"）
- 当前场景点高亮金色，已完成的点实心，未到达的点空心

### 3D 交互
- **鼠标拖拽**：旋转 3D 场景（camRotX, camRotY）
- **滚轮缩放**：调整视距（camZoom）
- **场景切换**：粒子平滑 tween 过渡到新位置

### 场景内按钮
每个场景有 1-2 个专属交互按钮（见各场景描述），触发场景内动画或模式切换。

### 术语表
- 右下角固定 ❓ 按钮：`position: fixed; bottom: 24px; right: 24px; z-index: 9999`
- 点击弹出术语解释面板，从右侧滑入
- 半透明深色背景，每个术语：英文（粗体） + 中文名 + 一句话解释

---

## 3D 渲染技术方案

沿用 #58 的成熟 3D 引擎，核心技术：

### 投影系统
```
// 3D → 2D 投影
function project(x, y, z) {
    // 应用 X 轴旋转（俯仰）
    let ry = y * cos(camRotX) - z * sin(camRotX)
    let rz = y * sin(camRotX) + z * cos(camRotX)
    // 应用 Y 轴旋转（偏航）
    let rx = x * cos(camRotY) + rz * sin(camRotY)
    rz = -x * sin(camRotY) + rz * cos(camRotY)
    // 透视投影
    let scale = focalLength / (focalLength + rz)
    return { sx: centerX + rx * scale, sy: centerY + ry * scale, scale }
}
```

### 粒子系统
```javascript
// 每个粒子
{ x, y, z, tx, ty, tz, color, size, alpha }
// tx/ty/tz 为目标位置，通过 lerp 平滑过渡
// 场景切换时更新所有粒子的目标位置
```

### 场景专属渲染
```javascript
// 每个场景有独立的额外渲染函数
function drawSceneExtra(ctx, time) {
    switch(currentScene) {
        case 0: drawServerRack(ctx, time); break;
        case 1: drawNVMeInternal(ctx, time); break;
        case 2: drawCPUFactory(ctx, time); break;
        // ...
    }
}
```

### 视觉特效
- **发光渲染**：径向渐变多层叠加（核心 44% → 中间 18% → 透明）
- **流动动画**：白色点沿连接线移动，sin 波调制亮度
- **星空背景**：180 颗随机闪烁星点
- **3D 网格**：透视校正的参考网格线

---

## 关键技术数据参考（已网络验证）

### NVIDIA H100 GPU
| 指标 | 数据 |
|------|------|
| SM 数量 | 80 |
| CUDA Cores | 16,896（每 SM 128 个 × 2 数据路径） |
| Tensor Cores | 528（每 SM 4 个，第四代） |
| HBM3 容量 | 80 GB |
| HBM3 带宽 | 3.35 TB/s |
| L2 Cache | 50 MB |
| Shared Memory / SM | 最大 228 KB（与 L1 共享） |
| Register File / SM | 256 KB |
| NVLink 4.0 | 900 GB/s 双向（18 条 × 50 GB/s） |
| Tensor Core 精度 | FP16/BF16 输入，FP32 累加 |
| 理论算力 (FP16) | ~990 TFLOPS |

### 通信带宽对比
| 通道 | 带宽 | 相对速度 |
|------|------|---------|
| Register File | ∞ | 基准 |
| Shared Memory (SRAM) | ~19 TB/s | ████████████████████ |
| L2 Cache | ~6 TB/s | ██████ |
| HBM3 | 3.35 TB/s | ███ |
| NVLink 4.0 | 900 GB/s | █ |
| PCIe Gen5 | 64 GB/s | ▎ |
| DDR5 | 90 GB/s | ▍ |
| NVMe SSD | 7 GB/s | ▏ |

### 训练内存占用（以 LLaMA 70B 为例）
| 组件 | 大小 (FP32) | 大小 (混合精度) |
|------|------------|---------------|
| 模型权重 | 280 GB | 140 GB (FP16) + 280 GB (FP32 master) |
| 梯度 | 280 GB | 140 GB (FP16) |
| 优化器状态 (Adam) | 560 GB | 560 GB (FP32) |
| 激活值 | 变化 | 取决于 batch size 和序列长度 |
| **总计** | **~1.1 TB** | **~1.12 TB** |

→ 单卡 80 GB 远远不够，必须多卡分摊（至少 16 张 H100）

---

## 与现有动画的差异化

| 动画 | 侧重点 | 视角 |
|------|--------|------|
| #38 GPU 并行计算 | SIMD、线程层次、并行原理 | 宏观概念 |
| #40 存算一体 | 存储与计算的架构融合 | 架构对比 |
| #05 硬件与芯片 | CPU/GPU/TPU 芯片对比 | 芯片级对比 |
| **#60 本动画** | **训练数据在硬件间的完整旅程** | **微观追踪，从磁盘到 Tensor Core 到多卡同步** |

本动画的独特之处：以**数据视角**的第一人称沉浸式体验，追踪每一跳的带宽/延迟差异，展示每个硬件组件的 3D 内部结构。

---

## 文件同步清单

创建完成后需要同步到两个目录：
- [ ] `19-ai-training-ppt/interactive/60-大模型训练数据硬件之旅.html`
- [ ] `docs/60-大模型训练数据硬件之旅.html`
- [ ] 更新 `19-ai-training-ppt/interactive/index.html`（数量 59→60 + 新卡片）
- [ ] 更新 `docs/index.html`（数量 59→60 + 新卡片）
- [ ] 验证术语表 ❓ 按钮功能正常
- [ ] 验证所有 17 个场景导航正常
- [ ] 验证 3D 旋转 / 缩放交互正常
- [ ] 验证所有场景内按钮交互正常
